{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}

    <div class="col-md-6 mx-auto">
        <h2 class="mb-2">Modifier votre mot de passe</h2>
        <a href="{{ path("app_account") }}" class="text-decoration-none">Retour a mon compte</a>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary w-100" formnovalidate>Modifier</button>
        {{ form_end(form) }}
    </div>

{% endblock %}