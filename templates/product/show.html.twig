{% extends 'base.html.twig' %}
{% block title %}{{ product.name }}_E-Camer{% endblock %}
{% block body %}

    <div class="row">
        <div class="col-md-4">
            <img src="/uploads/{{ product.illustration }}" alt="{{ product.name }}" class="img-fluid">
        </div>
        <div class="col-md-7 my-auto">
            <h3>{{ product.name }}</h3>
            <p>{{ product.subtitle }}</p>
            <span class="font-weight-bold text-center">{{ (product.price /100) | number_format(2,'.')}}€</span>
            <hr>
            <p>
                {{ product.decription }}
            </p>
            <a href="" class="btn btn-primary btn-sm">Ajouter au panier</a>
        </div>
    </div>
{% endblock %}
